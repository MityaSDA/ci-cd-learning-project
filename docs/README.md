# CI/CD Demo Project

## 📌 Описание проекта

**CI/CD Demo Project** — demo full-stack проект,
демонстрирующий полный цикл разработки, тестирования, контейнеризации
и деплоя backend-приложения на Python.

Проект создан с фокусом на:

- понимание CI/CD процессов
- контейнеризацию с Docker
- автоматическое тестирование
- версионирование приложения
- Documentation as Code

Проект используется как:

- учебный проект
- практическое портфолио
- пример работы технического писателя в CI/CD

---

## 🎯 Цели проекта

- показать полный жизненный цикл backend-приложения
- продемонстрировать работу CI/CD пайплайнов
- связать код, инфраструктуру и документацию
- отработать процессы Docs as Code
- создать портфолио для middle-уровня

---

## 🏗 Архитектура проекта (кратко)

Developer
|
v
GitHub Repository
|
v
GitHub Actions (CI)
|
v
Docker Hub (Images)
|
v
Render (Deployment)

yaml
Копировать код

### Поток работы

1. Разработчик делает `git push`
2. GitHub Actions:
   - запускает тесты
   - собирает Docker-образ
   - публикует образ в Docker Hub
3. Render получает новый образ и деплоит приложение
4. Документация собирается и публикуется автоматически

---

## ⚙️ Стек технологий

### Backend

- Python 3.10

- Flask — REST API
- pytest — автоматические тесты
- Gunicorn — production WSGI сервер

### DevOps / CI/CD

- GitHub Actions
- Docker
- Docker Hub
- Render

### Документация

- Markdown
- MkDocs
- markdownlint
- GitHub Pages

---

## 🔌 Возможности API

Backend предоставляет REST API для выполнения арифметических операций.

### Примеры эндпоинтов

| Метод | URL | Описание |
|------|-----|----------|
| GET | `/health` | Проверка состояния сервиса |
| GET | `/api/add` | Сложение чисел |
| GET | `/api/multiply` | Умножение чисел |
| GET | `/api/divide` | Деление чисел |

📌 Все ответы возвращаются в формате JSON  
📌 Используется единый формат успешных и ошибочных ответов  

Подробное описание API находится в `docs/api.md`.

---

## ▶️ Локальный запуск (без Docker)

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r backend/requirements.txt
python backend/app.py
```

Приложение будет доступно по адресу:

```!
http://localhost:10000
```

## 🐳 Docker

Сборка образа

```bash
docker build -t myapp -f backend/Dockerfile backend
```

Запуск контейнера

```bash

docker run -p 10000:10000 myapp
```

## 🔁 CI/CD (GitHub Actions)

CI/CD pipeline автоматически запускается при:

```!
push в ветку main

push любого тега

pull request в main
```

Pipeline включает:

```!
установку зависимостей backend

запуск pytest

сборку Docker-образа

публикацию образа в Docker Hub

триггер деплоя в Render

сборку и деплой документации
```

Конфигурация:

```bash

.github/workflows/
```

## ☁️ Деплой в Render

```!
Backend задеплоен как Web Service (Docker Image).

Источник: Docker Hub

Тип: Free instance

Порт: 10000
```

Пример URL:

```!

https://myapp-latest-b5gs.onrender.com
```

## 🏷 Версионирование

Используется семантическое версионирование:

latest — актуальная версия из main

vX.Y.Z — релизные версии

sha-xxxxxxx — сборки по commit hash

Создание тега

```bash

git tag v1.0.0
git push origin v1.0.0
```

## 📚 Документация

Документация ведётся по принципу Documentation as Code:

хранится в репозитории (/docs)

проверяется в CI

публикуется автоматически

### Разделы документации

архитектура

API

CI/CD

Docker

деплой

frontend

troubleshooting

глоссарий

## 📈 Статус проекта

Проект активно развивается.

## Планируемые улучшения

PostgreSQL

полноценный React frontend

мониторинг и алерты

blue-green deployment

security hardening
