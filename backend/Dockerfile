# Используем официальный базовый образ Python версии 3.10 
# Этот образ легковесный и подходит для продакшен-контейнеров
FROM python:3.10-slim

# Устанавливаем рабочую директорию внутри контейнера
# Все последующие команды будут выполняться здесь (/app)
WORKDIR /app

# Копируем файл зависимостей в контейнер
# (копируем отдельно для эффективного кэширования pip install)
COPY requirements.txt .

# Устанавливаем зависимости из requirements.txt
# --no-cache-dir экономит место в контейнере
RUN pip install --no-cache-dir -r requirements.txt

# Копируем весь проект внутрь контейнера
COPY . .

# Открываем порт 10000 для Render
# Render использует эту инструкцию, чтобы понять, какой порт слушает приложение
EXPOSE 10000

# Команда, которая будет выполняться при запуске контейнера
# В данном проекте мы запускаем Flask-приложение app.py
CMD ["python", "app.py"]