# CI/CD Learning Project — Middle-Level Python API

## 📌 Описание проекта

Это учебно-практический проект, демонстрирующий полный цикл разработки,

тестирования и деплоя backend-приложения на Python с использованием:

\- Flask API

\- Docker

\- GitHub Actions (CI)

\- Docker Hub

\- Render (Cloud Deployment)

Проект создан с фокусом на \*\*понимание CI/CD процессов\*\*, контейнеризацию,

автоматическое тестирование и версионирование.


## 🏗 Архитектура проекта (кратко)

Developer → GitHub → GitHub Actions → Docker Hub → Render

`              `│           │               │          │

`              `│           └─ Tests + Build └─ Images  └─ Deploy

`              `└─ Git push

Код хранится в GitHub

GitHub Actions запускает тесты и сборку Docker-образа

Docker-образ публикуется в Docker Hub

Render автоматически или вручную деплоит приложение

## ⚙️ Стек технологий

Python 3.10

Flask — REST API

pytest — автоматические тесты

Docker — контейнеризация

GitHub Actions — CI/CD

Docker Hub — registry образов

Render — облачный хостинг

## 🔌 API возможности

Приложение предоставляет REST API для управления задачами.

Примеры эндпоинтов:

Метод	URL	Описание

GET	/	Проверка статуса сервиса

GET	/tasks	Получить список задач

POST	/tasks	Создать новую задачу

PUT	/tasks/<id>	Обновить задачу

DELETE	/tasks/<id>	Удалить задачу

📌 Все ответы возвращаются в формате JSON.

## ▶️ Локальный запуск (без Docker)

bash

Копировать код

python -m venv venv

source venv/bin/activate  # Windows: venv\Scripts\activate

pip install -r requirements.txt

python app.py

Приложение будет доступно на:

arduino

Копировать код

http://localhost:10000

## 🐳 Docker

Сборка образа локально

bash

Копировать код

docker build -t myapp .

Запуск контейнера

bash

Копировать код

docker run -p 10000:10000 myapp

## 🔁 CI/CD (GitHub Actions)

CI pipeline автоматически выполняется при:

push в ветку main

push тега v\*

pull request

Pipeline включает:

Установку зависимостей

Запуск pytest

Сборку Docker-образа

Публикацию образа в Docker Hub

(Опционально) Триггер деплоя в Render

Файл конфигурации:

bash

Копировать код

.github/workflows/ci.yml

## ☁️ Деплой в Render

Приложение задеплоено как Web Service (Docker Image).

Источник: Docker Hub

Тип: Free instance

Порт: 10000

URL:

arduino

Копировать код

https://myapp-latest-b5gs.onrender.com

🏷 Версионирование

Используется семантическое версионирование:

latest — актуальная версия из main

vX.Y.Z — релизные версии

sha-xxxxxxx — сборки по commit hash

Теги создаются через Git:

bash

Копировать код

git tag v1.0.0

git push origin v1.0.0

## 📈 Статус проекта

Проект активно развивается.

Планируемые улучшения:

PostgreSQL

React frontend

Monitoring & alerts

Production WSGI (Gunicorn)

Security hardening


